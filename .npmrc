# Flat node_modules so every workspace package can resolve hoisted third-party
# deps without each package re-declaring them. Standard for app monorepos that
# consume internal packages as source (no publish step).
node-linker=hoisted
